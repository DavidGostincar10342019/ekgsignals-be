python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt za instaliranje biblioteka
pip freeze > requirements.txt -> pip freeze → izbaci spisak svih instaliranih 
paketa u aktivnom environment-u (sa tačnim verzijama).
 
python -m app.main pokretanje
